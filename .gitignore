# 构建产物
TabbedExplorer.exe
TabbedExplorer.new.exe
build.log
build.log.prev
*.pdb
*.user
.vs/
bin/
obj/

# 探针留下的截图 / 记录（源码 probe/*.py 要进库，图不进）
*.png
!docs/*.png
!assets/*.png
probe/*.log
probe/*.txt

# 本机数据（绿色便携那个 data\ 目录：settings.txt / desktops.txt / log.txt 全在这儿）
data/

# 开发时的项目记忆（.workbuddy\ 是工作区的本地笔记，不进公开仓库）
.workbuddy/

# 本机构建后钩子（个人镜像/部署脚本之类，不进公开仓库）
tools/*.local.bat

# 本机临时
*.tmp
