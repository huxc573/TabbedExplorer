# 变更记录

版本号唯一来源 = 仓库根 `VERSION` 文件（单行），标签 `v<X.Y.Z>`（附注标签）。
口径：新功能抬 MINOR，修 bug 只走 PATCH，不兼容才抬 MAJOR。

## [v1.12.0] - 2026-09-23 01:31

**主题：托盘菜单两处调整 + 书签文件夹专属图标 + 设置窗口露署名与开源地址 + 开源首发整理（MIT / 注释去过程叙述 / `.gitattributes` / 清一次性脚本）**

**1 / 托盘右键：去掉「记住当前标签」，换成两个管理器**
手动存标签本来就多余（标签有改动就攒 800ms 自己落盘，退出前还会再存一次）；
「书签管理器」「历史记录管理器」以前只在窗口里够得着，现在托盘上能直达。
⚠ 设置窗口里那条「立即记住当前标签」**保留** —— 只在托盘删。

**2 / 书签自己的文件夹换专属图标**
以前借的是**系统那颗黄色文件夹**，于是「书签里的分组」和「磁盘上真实的文件夹」在管理器和
书签栏上长得一模一样，一眼分不出谁是谁。现在自己画一颗：蓝文件夹（双色耳 + 描边）+ 右下角一颗金星。
画法是「先在 8 倍大的画布上画好、再缩到目标尺寸」—— 15px 这种尺寸上直接画没有抗锯齿的余地；
结果跟系统文件夹图标一样进 `ShellIcon` 的缓存（两个管理器是逐行调它的）。
颜色**故意不跟主题走**：它是「这是书签」的标识，深色浅色下得是同一个样子。

**3 / 设置窗口露署名与开源地址**
顶部「版本」下面一行写作者，再一行是**可点的开源地址**（点了交给系统默认浏览器打开）。
作者名与地址收成 `AppInfo.Author` / `AppInfo.RepoUrl` 两个常量，以后改地址只改这一处。

**4 / 开源首发整理**
- 加 `LICENSE`（MIT，`Copyright (c) 2026 huxc573`）；README 顶部补作者 / 开源地址 / 协议 / 版本；
  新增「来源与致谢」一节 —— 说明灵感来自 QTTabBar 那类工具、本仓库**不含它的任何代码**。
- 源码注释里 241 处带昵称的引用（`用户 2026-09-22：…` 这种过程叙述）清成中性说法，
  **「为什么这么定」的技术理由一句没删**；顺手把注释里出现的本机开发路径、本机虚拟桌面 GUID、
  示例桌面名换掉。
- 加 `.gitattributes`：库内行尾一律 LF，`.bat` / `.cmd` 显式钉 CRLF，图片与图标按二进制处理
  （这台机器 git 全局是 `core.autocrlf=true`，不钉死换台机器口径就变）。
- `.gitignore` 补 `.workbuddy/`（项目本地笔记不进公开仓库），并用 `git check-ignore -v` 逐条验收。
- 删掉 11 个一次性脚本（已定论的决策探针、一次性截图裁剪、一次性批量改文本），保留 18 个能复用的探针。
- README 里的「收藏夹栏」全部改成界面里的实际叫法「书签栏」，并补上两个管理器与专属图标的说明。

## [v1.11.0] - 2026-09-23 00:02

**主题：两个「代码写了、但从没接上」的 bug + 7 条优化（设置滚动条深色、通知精简、Debug 开关与清理日志、批量关标签不再闪、内存、两个管理器多选、历史按日期整组删）**

用户这一批是 2 条 bug + 7 条优化。

**Bug 1 / 点书签栏文件夹，弹出来的子书签 / 子文件夹点不动**
日志把结论钉死了：同一份菜单，走 `Defer`（推后一轮）才弹的那几个入口一直好使，
而**在 `MouseUp` 里同步弹**的那些时好时坏 —— 书签栏子菜单弹了 17 次、只命中 1 次。
根因是鼠标捕获：调用方在 `MouseUp` 里弹菜单，那一刻 WinForms 还攥着那个控件的捕获，
**它会在事件处理返回之后才把捕获放掉** —— 正好把刚弹出来那个菜单窗口的 `Capture = true` 抢走。
菜单一没捕获，鼠标消息就散了：悬停不亮、点了不响应，看门狗过一会儿再把它收掉。
根治收在**一个地方**：`PopMenu.Show` 一律 `BeginInvoke` 推后一轮再开窗。
以后新增调用点不必各自记这条规矩。（看门狗的自愈次数 3 → 8，作为兜底。）

**Bug 2 / 按钮区 + 内容区 + Shift 滚轮「都没生效」**
不是没生效，是**从来没装上**：`MouseWheelHook` 这个类全项目谁都没 `new` 过 ——
`WheelRouter` 的登记表、`TabStrip.InButtonArea` 的判定、`EmbedForm` 的回调全都写好了，
就是没人把钩子装起来，所以三个入口一起不动、且失效得很彻底。
现在在 `DesktopHub` 里装一次（全进程共用），`EmbedForm` 关窗时摘掉登记项。
README 里那张「滚轮怎么用」的表从这一版起才是真的。

**优化 1 / 设置窗口那条滚动条跟着颜色模式**
`TabPage.AutoScroll` 画的滚动条属于**非客户区**，不吃我们自绘的配色，系统永远按浅色画 ——
深色下就是页面右边竖着的一条白。`Theme.StyleScrollBar` 把 uxtheme 的子应用名
（`DarkMode_Explorer` / `Explorer`）套在**拥有滚动条那个窗口**上，跟 shell 视图用的是同一个机制；
页里的子控件也逐个设一遍，免得出现「滚动条深了、勾选框还是浅的」。

**优化 2 / 页面已经有反馈的动作不再弹右下角气泡**
书签栏拖入、管理器「添加书签」/拖入、清空历史 —— 这些动作的结果**当场就在栏上或列表里看得见**，
气泡是多余的。「添加到书签」那一处留着一条判据：**书签栏是关着的时候才提示**
（那时界面上真的什么都没发生）。复制类和错误类一律保留 —— 那些不说一声就真看不见。

**优化 3 / 日志写不写由「Debug 模式」决定 + 清理日志按钮**
`Diag.Enabled` 总闸，值来自 `settings.json` 的 `debug`（默认 `false`，本机开着）。
关着的时候一个字节都不写（连目录都不建）；开着照旧每一步落盘。
开机那十来行是个例外 —— 设置还没读到、也最可能崩的那一段，痕迹留着。
启动时日志超过 1 MB 自动轮转成 `log.txt.prev.txt`（只留一份）。
设置窗口里加了「Debug 模式」勾选框 + 一行实时大小 + 「清理日志」按钮（清完按钮上那个数字当场归零）。

**优化 4 / 批量关标签时当前画面不再闪**
两个原因。① 主因：`CloseTab` 本来**每关一个就 `Activate` 一次**，批量关就成了
「关张三 → 显示李四 → 关李四 → 显示王五 → …」，每一下都是把一个真 explorer 窗口现出来再藏掉 ——
现在这条路上中间的 `Activate` 全按掉，循环跑完只定一次。② `ExplorerHost.Close` 还回桌面之前
忘了 `SW_HIDE`：窗口一直带着 `WS_VISIBLE`（嵌进来时 `SHOW` 过），非激活标签是靠**父面板**
`Visible = false` 藏的 —— 那清不掉子窗口自己的 `WS_VISIBLE` 位，于是一 `SetParent(桌面)`
它就在屏幕上现身一瞬。

**优化 5 / 内存**
- `ShellIcon.PathIcon` **完全没有缓存**，而两个管理器是在 `OnPaint` 里**逐行**调它的 ——
  窗口一动就是几十次 `SHGetFileInfo`（碰磁盘）+ 几十张新位图；原先唯一的缓存是文件夹图标，
  而且是**单槽**的（按宽度算命中），标签用 16、管理器用 15/16，几个尺寸来回一套就顶掉，等于没有。
  现在一张表按「尺寸 + 路径」缓存（上限 400 条，超了整表丢），位图不 Dispose、交给 GC。
- 我们自己这个进程也收一次驻留内存（`EmptyWorkingSet`，跟标签那一条同机制、只是目标是自己）：
  切完标签停留 3 秒后一次、窗口收进托盘时一次。

**优化 6 / 两个管理器都能多选**
书签管理器的右列 + 历史管理器那张表：点 = 只选它，Ctrl+点 = 加减，Shift+点 = 选一段，
Ctrl+A 全选，Esc 取消；多选中的行左边一条强调色短竖条，标题上显示「已选 N」。
删除按钮和右键菜单都作用于整份选择，而且**一次 `Edit` / 一次落盘**（不是一条写一次盘）。
书签那边的「删除这一项」→「删除所选」，没多选时还是删左树选中那个文件夹（老行为不变）。

**优化 7 / 历史管理器可以按日期整组删**
右键点在日期分堆标题上 → 「删除「今天」这一组（N 条）」，一次把 `history.json` 里那一堆全删掉
（只删记录，磁盘上的文件夹一个字节不动）。左键点标题 = 这一组全选中。
`History.RemoveMany` / `History.PathsOfDay` 是配套加的两个口。

**顺手修的小毛病 / `favorites.json` 不是合法 JSON**
`FavStore.Save` 写 `_note` 那一行时**漏了一个逗号**，所以 `data\favorites.json` 严格来说不是合法
JSON（另外三个 —— `settings` / `desktops` / `history` —— 都是）。我们自己的读取器容得下，
所以功能上一直没露过破绽；但用户说过这几个文件是**方便手改**的，用编辑器一打开就报错。
补上那个逗号。

## [v1.10.0] - 2026-09-22 23:18

**主题：书签（原「收藏夹」）「全部打开」与拖拽整理 + 历史记录按日期归类 + 标签条两处布局修正**

用户这一批是 1 条 bug + 4 条优化，外加一句「字体是不是变大了」的追问。

**问：字体变大了吗 —— 没有，变大的是窗口**
`git diff v1.8.0 HEAD -- src/` 里 `new Font(...)` 一行都没动（0/7 个文件）。
真正变大的是 v1.9.0 给 `FavManagerForm` 做的那次修复：把尺寸常量从**裸像素**换成 `Px()`
（150% 屏上 ×1.5）—— 窗口 880×560 → 1320×840 设备像素，行高 30 → 45。
那本来是为了修「文字堆叠错位」（小标题与首行挤在同一行），修得对，但顺带把密度拉散了。
这一版把**逻辑尺寸**调紧一档（行 30→24、标题条 40→34、窗口 920×560 → 860×500 等），
字号一个没动 —— 跟原生资源管理器一致。

**Bug 1 / 标签占满后「关闭」和「加号」堆叠**
`TabStrip.EnsureLayout` 里 `areaRight` 已经扣过一个 `NewButtonWidth`（给「+」留位），
后面算 `limit` 时**又扣了一次**，于是「+」被硬推到最后一个标签的 ✕ 底下。
改成 `limit = areaRight`。

**优化 1 / 选中标签那条蓝线挪到标签底部**
原来画在标签顶边，跟顶部的滚动条轨道抢同一排像素。现在贴底画，并且**画在分隔线之后**
（否则会被压在 `Height-1` 的那条线盖掉一像素）。

**优化 2 / 「收藏夹」改叫「书签」+ 文件夹「全部打开（N 书签）」**
面向用户的文案统一改名（171 处）；代码标识符 `FavStore` / `FavBar` / `favbar` / `favorites.json` 一律不动。
老数据里那条栏的根名从「收藏夹栏」自动迁移成「书签栏」（只认同旧默认名，他自己改过的不动）。
文件夹（含子文件夹）右键新增「全部打开（N 书签）」，N 是**递归**数出来的；**超过 7 项先弹一句确认**
（用户：「超过 7 时需要再次确认避免误触」）。确认框是自绘的 `Confirm`（不用 `MessageBox`，深色下不惨白）。

**优化 3 / 书签栏和管理器都能拖着整理**
两处同一套做法：按下只记状态 → 位移超过 4px 才算拖动 → 松手才真的改数据（`FavStore.Move`）。
- 书签栏：拖动项的「打开」动作**从 MouseDown 挪到 MouseUp** —— 不挪就没法跟拖动区分，
  而且弹菜单那条硬规矩本来就要求松手之后再弹（按着键弹，一松手捕获被收走 = 菜单闪一下）。
- 管理器：行**中间**落下 = 放进这个文件夹（选中罩一层蓝），行**上/下边缘**落下 = 同层调顺序（画一条蓝线）；
  `Move` 里挡掉了「挪进自己 / 自己的后代」和「把栏根挪出顶层」这两种会把树搞坏的动法。

**优化 4 / 历史记录按日期归类**
原来 `History` **故意不带时间戳**（当时的理由是「挑一个再去一次」，不是考古）——
要按日期归类就必须有时间，所以每条加上 `at`（本地 `yyyy-MM-dd HH:mm`）。
菜单和管理器左侧都按「今天 / 昨天 / 9月20日 / 更早（时间未知）」分堆，行尾显示 `HH:mm`。
`history.json` 改成 `{ "path": …, "at": … }` 对象数组，**老格式（裸字符串数组）照样读得进来**，`at` 留空。
文案修正：「（还有 N 条更早的，看 data\history.json）」→「（还有 N 条更早的，打开管理器查看）」。

新增文件：`src/Confirm.cs`（自绘确认框）、`src/FavActions.cs`（书签共用动作）。

## [v1.9.0] - 2026-09-22 22:47

**主题：修「标签文字画到右边按钮上」（GDI 文本不吃 GDI+ 裁剪）+ 历史记录管理器（仿收藏夹管理器）+ 标签条滚动条移到顶部拉满宽度 + 收藏夹管理器行距**

用户这一批是 1 条 bug + 3 条优化。

**Bug 1 / 标签栏和功能图标堆叠 —— 不是布局算错，是"画出去"的**
`TabStrip.OnPaint` 里明明 `g.SetClip(标签区)` 了：标签背景、文件夹图标都规规矩矩被裁掉，
**唯独文字越界**。铁证是越界的那几个「此电脑」**只有文字、没有图标**（图标走 `Graphics.DrawImage`，被裁了）。
根因：`TextRenderer.DrawText`（GDI 那套）**默认不吃 `Graphics` 的裁剪区**，只认传给它的那个 `bounds` 矩形；
要它跟着 GDI+ 的裁剪一起裁，必须显式加 `TextFormatFlags.PreserveGraphicsClipping`。
加上之后第 6 个以后的标签彻底画不出东西，堆叠消失。
取证靠新增的 `probe/grab_child.py` —— **抓子窗口自己的 DC**。
⚠ 别用父窗口整张 `PrintWindow` 判断：`PW_RENDERFULLCONTENT` 走 DWM，
同一程序有第二个窗口时会把别人的内容混进来（第一版就是这么被误导的）。

**优化 1 / 滚动条挪到顶部 + 拉满整条标签条宽度**
`LayoutScrollBar` 的轨道从「标签区宽度、贴底」改成「整条宽度（`0..Width`）、贴顶（`y=0`）」。
平时仍然隐藏，鼠标进标签条才显示。画和命中判定共用这一份几何 —— 不会出现「看到的滑块和点得着的差几像素」。

**优化 2 / 管理收藏夹：标题栏没图标 + 点文件夹后文字堆叠错位**
- 图标：这个窗口从来没设过 `Icon` ⇒ 补 `ShellIcon.AppIcon(false)`（`InputBox` 那个小对话框也一样）。
- 文字堆叠是两个毛病叠在一起：
  ① 小标题（「收藏夹」/ 选中的文件夹名）画在 `TopH + Px(4)`，第一行却从 `TopH + Px(6)` 开始
     ⇒ **同一行**、字压字（截图里的「收藏夹收藏夹栏（收藏…」「新建文件夹新建文件夹」就是这个）；
  ② 整张窗体的尺寸常量原来是**裸像素**没乘 DPI：150% 下 `TopH=40` 装不下高 `Px(26)=39` 的按钮
     （按钮溢到标题行上），行高 26/30 设备像素也只有 17/20 逻辑像素，文字上下贴边。
  现在 `TopH`/`LeftW`/`TreeRowH`/`ListRowH` 全部走 `Px()`，并给标题留出 `CaptionH`，行从 `TopH + CaptionH` 开始。

**优化 3 / 「打开历史记录文件」改成「打开历史记录管理器」；点历史图标菜单闪一下就没了**
- 新窗口 `HistoryManagerForm`（版式跟收藏夹管理器一套）：顶上搜索框 + 一排动作按钮
  （打开 / 复制完整路径 / 删除这一条 / 打开 json / 清空历史）；左边一条历史表（新的在前、行尾 ✕ 删一条）；
  右边是选中那一条的详情（完整路径 / 类型 / 在历史里的位置）。只动 `data\history.json`，**磁盘一个字节不碰**。
  `History` 补了 `Remove`，菜单那条从「把 json 丢给记事本」换成管理器入口。
- 菜单闪退 —— 日志把它钉死了：**点图标那条路每次都活 ~294ms** 就被关（`菜单: 弹出` 之后紧跟 `菜单: 关闭`），
  而快捷键那条路能活到用户自己点条目（2.3s / 8.7s / 4.2s）。294ms ≈ 菜单看门狗的 250ms。
  为什么：功能按钮的事件原来发在 **`MouseDown`** 里 ⇒ 菜单弹出来那一刻**左键还按着**；
  我们的菜单窗口不是前台窗口，Windows 只允许「有按键按着的时候」抓着鼠标捕获 ⇒
  用户一松手捕获当场被系统收走 ⇒ 看门狗以为被外人抢了，把菜单收了。
  右键那两份菜单不受影响，因为它们的规矩本来就是「**在 `MouseUp` 里弹**」。
  修法两条：① 功能按钮改成**松手才发事件**（跟右键一条规矩）；
  ② 看门狗宽容一点 —— 捕获没了但**鼠标还在菜单里**就先把捕获抢回来（最多 3 次），
  鼠标真在外面才收摊。根治靠 ①，②是防止以后再有人从「按下时弹菜单」的入口进来又复发。

**待用户自验**
① 标签多到溢出时，右边那排功能按钮上不再叠字；② 滚动条在标签条**顶部**、拉满宽度；
③ 管理收藏夹的窗口图标 + 行距/标题不再压字；④ 历史记录管理器；⑤ 点历史图标菜单不再闪退。

## [v1.8.0] - 2026-09-22 22:20

**主题：右键菜单改成自绘（两代 WinForms 菜单都不兼容「宿主里嵌着外来子窗口」）+ 防闪提前到窗口创建那一刻 + 收藏夹管理器（仿浏览器）+ 标签条滚动条**

用户这一批是 4 条验证 + 1 条 bug + 5 条优化，全做完了。

**验证 1 / 历史记录「点开后所有功能都不可用」——找到真根因了**
不是「点了没反应」，是 **UI 线程卡死**。探针实测（`probe/menu_smoke.py` 之前的诊断）：
主窗口连 `WM_NULL` 都不回。两代实现两种死法：
- 老的 `ContextMenu` + `MeasureItem`：`Show()` 是**模态**的，模态期间**主窗口被整个禁用**，
  而菜单项又收不到点击 ⇒ 菜单关不掉、窗口一直是死的。用户那句「所有功能都不可用」就是这个。
- 换 `ContextMenuStrip` 之后更糟：直接真卡死（79 次弹出只关了 78 次，且一条 `菜单项:` 都没有）。

为什么两套都失灵：这个程序的主窗口里**嵌着别的进程的子窗口**（每个标签一个 explorer.exe），
而 ToolStrip / ContextMenu 那套弹窗依赖「抢鼠标捕获 + 转前台 + 模态过滤链」——
在「宿主窗口里有外来子窗口」的情况下会被搅乱。
现在**自己画**（`PopMenu.cs` 重写）：无边框 `Form` 当菜单，非模态 `Show()`（绝不进嵌套消息循环），
自己 `Capture` 收鼠标并分发到命中那一层（点菜单外面 = 关掉，跟原生一样），自己 `OnPaint`（深色不漏白底），
子菜单 = 挂在上一层名下的另一个窗体。另加一道**看门狗**：捕获被别人抢走就自动收摊（防菜单粘屏）。
验：新增 `probe/menu_smoke.py`（**只用 PostMessage，不动光标、不抢前台**）跑通了 3 份菜单 ——
收藏夹栏右键（4 项）、标签右键（14 项）、历史记录（17 项），三份都是「弹出 → 菜单项 → 关闭」
完整三行、且全程主窗口一直能应答。

**验证 2 / 从桌面 / 开始菜单开文件夹还是闪**
v1.7.0 把「藏」挪到专用线程 + 一起听 `CREATE` 了，但**还是闪**。日志给出答案：
每条都是「事件后 **0ms**，当时**已可见**」—— 反应已经最快了，可系统报给我们的时候窗口早画在屏幕上了。
两个错：
- 听 `CREATE` 那条路**被判定拦掉了**：藏这件事原来复用 `IsCapturable`，而它有一句
  「不可见又没被我们藏过 → 不算数」，CREATE 那一刻窗口本来就没可见 ⇒ 永远被判「不用管」。
  现在**藏和收用两把尺子**：藏走 `IsHideCandidate`（不要求可见），收仍走 `IsCapturable`。
- 光 `SW_HIDE` 本来就挡不住。再加一层兜底：**窗口一创建就把它置成全透明**
  （`WS_EX_LAYERED` + alpha=0，`EmbedApi.MakeTransparent`）——之后 explorer 自己那次 `ShowWindow`
  出来的一帧也是透明的，肉眼什么都看不到。
  ⚠ 收编进标签 / 放它回桌面时**必须** `ClearTransparent`，忘了就是「嵌进来的窗口永远是隐形的」；
  这条已经在 `ExplorerHost.AttachWindow`、卸载路径、`AdoptWindow`、`ReleaseIfAbandoned` 四处兜住。
  验：新增 `probe/exstyle_audit.py`（只读）确认没有任何窗口残留全透明。

**验证 3 / 右上三颗窗控图标已经好了，收藏夹图标偏小**
收藏夹那枚星（E734/E735）的 ink 天生比齿轮 / 历史 / 恢复小一圈，同一个字号并排看着就是小。
标签条上那枚单独给大一档（12px → 14px 字形字体），收藏夹栏左边那枚 18 → 20。

**Bug / 过长标签的遮挡不在滚动范围内**
`EnsureLayout` 里「自动加宽」那条路原来还把宽度**再夹一次**「标签区可用宽度」，
超长名字的标签被硬切成 `avail` 宽 ⇒ `total == avail` ⇒ `maxScroll` 只剩 4px，
**被吃掉的那截滚也滚不出来**。现在不夹了：名字多长标签就多宽（上限 `TabWidenMax`），
超出的部分交给横向滚动。顺带让**选中的标签自动滚进可视区**（`ScrollActiveIntoView`），
关标签也不再粗暴把 `scrollX` 归零。

**优化**
1. **收藏夹栏图标**换成「已打开收藏夹栏」那枚蓝色实心星；栏上右键加**重命名**（只改 json 里的显示名，
   磁盘上那个文件 / 文件夹一个字节不动）。
2. 「打开收藏夹栏数据目录」→ **「管理收藏夹」**，新开一个**仿浏览器的收藏夹管理器**
   （`FavManagerForm`）：左边文件夹树（可无限嵌套）、右边该层内容、顶部搜索 + 动作条，
   双击开成新标签、拖文件夹进来即嵌套、右键重命名 / 删除 / **设为收藏夹栏**。
   数据层跟着从「一串路径」重构成**树**（`FavNode`：`Name`/`Path`/`Bar`/`Kids`），
   `favorites.json` 缩进对齐、键名就是 `bar`/`name`/`children`，方便手改；
   老的一串纯路径格式读进来自动包成「收藏夹栏」文件夹（他那一份不用手工改）。
   托盘 / 设置窗口也加了「管理收藏夹…」入口。
3. **标签条滚动条**：平时隐藏，鼠标进标签条才出现；**轨道可点（滑块跳过去）、滑块可拖**
   （带抓取偏移，拖出标签条也跟手），悬停 / 拖动时颜色加重。画和命中判定共用同一份几何（`LayoutScrollBar`）。
4. **首次打开变慢**：串行队列是按入队顺序起 explorer 的，而还原是照记忆文件里的顺序来的 ——
   「当时选中那个」可能排在最后。现在**先建它**（抢到队列头名），全建完再 `MoveTabSynced` 挪回原位，
   所以标签顺序不变、但该激活的那个最先出来。
5. **设置窗口**：① 窗口高度**封顶在工作区内**（原来 `Math.Max(Px(240), 剩余)` 会把窗口顶出屏幕，
   底下那截永远看不见），装不下的交给页内滚动，两页一律 `AutoScroll`；
   ② tab 头带右边那一截空白由系统画、深色下是一条白 —— 新增 `TabHost`（`TabControl` 子类）
   在 `WM_PAINT` 之后补一刀底色，配色跟主题走。

## [v1.7.0] - 2026-09-22 21:12

**主题：标签条溢出/加宽的三个老毛病 + 外部打开不再闪 + 右键菜单功能补齐 + 设置窗口分「常规/快捷键」两页（热键可改）**

用户这一批是三条 bug + 五条优化 + 两条新增，都做完了。

**Bug 1：标签条溢出 / 加宽 / 滚轮（`TabStrip`）**
- **「名称过长时自动加宽」原来压根加不动宽**：加宽的上限被写成了 `TabWidth` 自己
  （`min(MaxTabWidth, need)`），名字再长也超不过「标签页宽度」那一项。
  现在 `TabWidth` 是**基准宽度**：名字放得下就一样宽，放不下才往上加，最多 400 逻辑像素，
  再夹一次「标签区可用宽度」（一个超长名字不许把别的标签全挤出去）。
- **标签和右边那排按钮「不在同一层」**：标签溢出时最后几个标签的矩形是伸到按钮底下的
  （画的时候裁掉了，看着像压在按钮下）。现在**裁剪线同时是命中线** —— 画和点都用 `TabsClipRight`，
  落在按钮区的点击/右键不再被当成「点到了那个被裁掉的标签」。
  （这也顺手修了「右边空白右键菜单没反应」：在按钮左边那块空白上右键，原来命中的是被裁掉的半个标签。）
- **滚轮**：标签上滚轮 = 切前后标签；**右边那排按钮上滚轮 = 横向滑标签条**
  （鼠标位置分派，`WheelTarget.StripWheel` 带 x）；标签放不下时底部多一条**位置指示条**。
  内容区的滚轮维持原样（放不下才接管，否则交给 explorer），并在文档里写明
  **explorer 文件列表原生就支持 Shift+滚轮 = 横向滚动**（我们放行时就是他自己的行为）。

**Bug 2：从桌面 / 开始菜单 / 自带收藏夹打开文件夹还是会闪一下**
根因不是「没藏」，是**藏得晚**：`WINEVENT_OUTOFCONTEXT` 的「某某窗口显示了」回调是投到
**注册钩子的那个线程**的消息队列上的 —— 原来注册在 UI 线程，UI 线程一忙（菜单模态循环、起 explorer、
算布局）这条通知就排队，而那个窗口**已经画在屏幕上了**，看到的就是闪一下。
现在给捕获监听**单开一条专用线程**（`WinShowWatcher.StartDedicated`），它只干这一件事、永远是空闲的，
回调一进来就**当场 `SW_HIDE`**（不再 `Post` 回 UI 线程），记账的活才回 UI 线程做；
顺带把 `EVENT_OBJECT_CREATE` 也听上（有的窗口创建时就带 `WS_VISIBLE`，CREATE 比 SHOW 早）。
日志里现在直接打**反应时间**（`事件后 Xms`），快不快一眼可查；藏窗口的登记集合改成加锁访问（两个线程都会碰）。

**Bug 3：标签页 / 右边空白的右键菜单「功能没实现」**
菜单其实弹得出来（日志有「弹出/关闭」），问题在「点了之后什么都看不出来」+ 右边空白点不到。
两处都收掉：菜单项统一从 `MenuFx.Item` 建，**点下去先在日志里留一行 `菜单项: xxx`**
（以后「弹了但没反应」和「点了但动作失败」在日志里是两回事）；右边空白的命中修正见 Bug 1。

**优化**
1. 最小化/最大化/关闭三颗图标太大：MDL2 里这三个字形（E921/E922/E923/E8BB）是填满 em 框的，
   12px 下 ink 就有 12x12（原生标题栏是 10x10）。单独给它们一套 10px 的字形字体
   （`probe/caption_glyph_size.py` 量过：10px → ink 10x10，正好对上原生）。
2. 托盘右键的「设置」菜单末尾加 **「更多选项…（打开设置窗口）」**；菜单里不再排
   数值输入（标签页宽度）和纯说明行（`Node.WindowOnly`）—— 那些进窗口才顺手。
   设置窗口收到 `DesktopHub.OpenSettings`（**全进程只开一个**，齿轮/空白右键/托盘菜单都走它）。
3. 标签页宽度改成**可直接输入数值**（`NumericUpDown`，64~240，步长 8），不再是预设值子菜单。
4. 设置窗口**打开时标题栏那条白杠**：`DwmSetWindowAttribute` 光调一次不管用 —— DWM 是窗口合成完成后
   才按属性画框，设早了被忽略（所以切出去再切回来才对）。现在 `OnHandleCreated` 就设 +
   每次 `WM_NCACTIVATE` 再补一枪，并且设完补一条 `SWP_FRAMECHANGED` 逼框架重算。
5. 「资源管理器左上角功能抓不回来」这行字**从程序里去掉**了，只在 README 的「已知取舍」里留着。

**新增**
1. 标签右键加 **「关闭其它标签页 / 关闭左边标签页 / 关闭右边标签页」**（标签条空白处右键也有一份，
   作用于当前标签）；做不到的那条（没有左/右邻居、只剩一个标签）直接置灰。
2. 设置窗口拆成**「常规」+「快捷键」**两个页（自绘 Tab 头，深色下不出现系统那种浅色标签）：
   原来那些设置项归到「常规」；「快捷键」页可以改程序自有的 7 条热键
   （新建/关闭/前后标签/历史/恢复/收藏夹栏），点方框直接按键捕获，撞车/非法当场提示，
   存 `settings.json` 的 `hotkey_*`（只写改过的那几条），另有「全部恢复默认」。
   `WinEHook` 的判定改成查这张表（`Hotkeys.Match`：整数比较，不分配内存保持钩子轻量），
   焦点在我们自己控件上时那条兜底路（`OnPreviewKeyDown`）也走同一份绑定 —— 不会出现「两边不一致」。
   Ctrl+1..9 固定不可改（一绑就是一串键），文档里写明。

## [v1.6.1] - 2026-09-22 20:38

**主题：修 v1.6.0 引入的两个「显示不全」（自绘菜单项没给宽度 + 悬停提示按一行量尺寸）**

用户报：所有右键菜单、悬停显示都显示不全，上个版本不会。查下来是 v1.6.0 的「自绘菜单」和
「自己量提示尺寸」各引入一个，机制都很明确：

1. **右键菜单的文字全被截**。`MenuFx` 自绘菜单项时只填了 `MeasureItemEventArgs.ItemHeight`，
   **没填 `ItemWidth`** —— WinForms 是拿这两个值定菜单尺寸的，而 `MeasureItemEventArgs` 的初值是 **0**；
   宽度留 0 ⇒ 菜单项窄成一条 ⇒ 文字被截。
   （老版本菜单由系统画、系统自己量，所以不会。自绘是 v1.6.0 新加的。）
   现在宽度也自己算：固定勾选列 + **这张菜单里最宽那一项**的文字 + （有子菜单时的箭头位）+ 右边距；
   取「整张菜单同一个宽度」而不是各算各的，否则 hover 那块底色会一块宽一块窄。
   顺带把「量」和「画」统一到同一套字体（不用 `DrawItemEventArgs.Font`，改成自己建一个
   Segoe UI 12px 的实例两边共用）—— 字体对不上就会量少了，还是会被截。
2. **标签悬停提示被截**。`Theme.OnTipPopup` 算高度时把路径那行按**一行**算（`SingleLine` 量），
   但画的时候是 `WordBreak` 折行的 —— 长路径折成两三行就需要更高，框子只有一行高 ⇒ 下半截被切。
   现在：宽度先夹到上限（屏幕放得下、最长 560），**再按夹好之后的宽度折行量**第二行的高度。
   另外单行提示也一起量（第一行是粗体，系统按常规字体量出来的宽度差几个像素，会把最后一个字切掉）；
   名字长过上限时用省略号收尾，不再硬切一半。
   还顺手改掉一处「猜原文」：原来用 `ToolTip.GetToolTip()` 取文字，而标签条是走 `Show(text, ...)` 弹的，
   读到的未必是正要显示的那串；现在由调用方在弹之前把原文递进来（`Theme.TipText`）。
   每次量完会往日志写一行 `提示: 量到 "xxx" = WxH（首行 …，次行 …，上限 …）` —— 万一还不对，直接看数字。

## [v1.6.0] - 2026-09-22 20:22

**主题：数据全改 JSON + 右键菜单复活 + 收藏夹自己管 + 自适应宽度拆两档 + 深浅色来回切不再花屏**

### 修的bug

1. **只有 `settings` 用了 JSON，桌面记忆/历史记录还是 txt**。现在四个数据文件统一走 JSON：
   `settings.json` / `desktops.json` / `history.json` / `favorites.json`；
   零依赖的手写解析器 `Json.cs` 由四边共用。老 `desktops.txt` / `history.txt` 第一次跑时读进来、
   原文件改名成 `.txt.migrated` 留底。`log.txt` 保持 txt —— 它就是给人看的。
2. **空白处右键、标签右键都没反应**。真凶：菜单弹在 `MouseDown` 里，
   紧接着到达的右键**抬起**消息被刚弹出的模态菜单（它持有鼠标捕获）收走，菜单于是立刻自己关了。
   全部改到 `MouseUp` 里弹；并且所有弹出统一走 `MenuFx.Show` ——
   **弹之前 / 关之后各打一行日志**，下次再有「点了没反应」直接看日志，不用猜。
3. **历史记录点了不开目录**。历史项的回调原来挂着空实现。现在接 `OpenFromHistory`：
   去重、不可还原的项（回收站/搜索结果页）不给点、点了有日志。
4. **从桌面 / 开始菜单打开文件夹还是闪一下**（Win+E 那条路不闪）。
   原因是「发现窗口」和「藏起来」之间隔了一次轮询。`TryCapture` 改成**一发现立刻 `SW_HIDE`**，
   并记下「这个是我藏的」；万一最后没被认领（不是我们的窗口），
   `ReleaseIfAbandoned` 会把它显示回去 —— 不能因为藏错了就在桌面上留个看不见的窗口。
5. **标签标题不该加粗**。标签用常规体；悬停提示的**第一行**（文件夹名）才加粗，路径不加粗。
6. **颜色模式切来切去会「一半深一半浅」**。两个原因一起修：
   ① `OnThemeChanged` 碰到「跟随系统」直接 `return` ——「强制深色 → 跟随系统（而系统是浅色）」
   这一步只把外壳刷成浅色、嵌进来的 explorer 还停在深色。
   现在**每次都重上一遍**（「跟随系统」不是「不用管」，它只是把目标值换成当前系统值）。
   ② `SetWindowTheme` 只在子应用名**真的变了**时才让窗口重画，来回切会撞上「目标值=现状」那一步
   （它觉得无事可做，旧像素就留在屏幕上）。现在先设成**反的那一边**再设目标，
   保证这一轮真发生两次切换；最后再对整棵子树 `RedrawWindow`（异步，不等它画完）。
   预热好的备用窗口也一起重上（它不在 `hosts` 里，漏了就是下个新标签页颜色不对）。
7. **设置里的「记住当前标签」说不清自己是干嘛的**（用户直接问了）。标签改成
   「立即记住当前标签（平时自动记，这个是手动存一次）」。

### 增强

1. **自适应宽度拆成两档**（原来一个开关管两件事，说不清）：
   「名称过长时自动加宽」（按每个标签各自的标题宽度算，夹在最小/最大之间）、
   「挤不下时自动缩窄」（不够就均分）。
   **两个都关**的时候不再横向溢出：标签总宽被夹在「右边那几个按钮之前」，
   放不下的靠横向滚动 —— 不会盖住设置按钮。
2. **滚轮**（原来在哪儿都不灵，因为内容区是**跨进程**嵌进来的窗口，
   滚轮消息根本进不了我们的消息循环）：加了全局低级鼠标钩子 `MouseWheelHook`，
   按 `WindowFromPoint` 判断那个窗口归谁 ——
   · 在标签条上 = 切上一个/下一个标签；
   · 在内容区、且标签条有溢出 = 横向滚动标签条。
   登记/查表走 `WheelRouter`；钩子线程只做「读状态 + 投回 UI 线程」，绝不碰界面。
3. **收藏夹栏自己管数据**（原来直接读系统 `%USERPROFILE%\Links`）：
   数据落在 `data\favorites.json`，第一次跑从系统收藏夹**播种**一次，之后自己记。
   最左边加了一枚收藏夹图标（点开数据目录）；**可以把文件夹 / 文件直接拖进来**；
   栏上右键可「在新标签页打开 / 用默认程序打开 / 复制完整路径 / 从收藏夹移除 / 刷新 / 打开数据目录 / 隐藏」。
4. **非重要变更不再弹右下角提示**。改设置、存记忆这类成功路径全部静音，只有**出错**才弹 ——
   这样弹出来就一定是真出事了。
5. **非激活标签省内存**：切走 3 秒还没被切回来的标签，对它那个独立 explorer 进程
   `EmptyWorkingSet`（只把驻留页换出，进程 / 窗口 / 里面的状态一点都不动）。
   收编来的窗口不碰 —— 那种往往跟桌面外壳共用进程，动了整台机器都有感觉。
   延时 3 秒是为了别在 Ctrl+Tab 来回切时反复「收了又读回来」。

### 菜单对齐

「显示收藏夹栏」这项原来靠文字前缀 `✓ ` 表示勾上 —— 比同级项多两个字符，看着就是没对齐
（用户原话「显示收藏夹里没和其它选项一样居左对齐」）。改成 `MenuItem.Checked` +
自绘的**固定宽度勾选列**（`MenuFx`），勾和不勾的文字起点完全一致，
顺便把「深色下系统那个小方块勾了看不见」一起解决。
⚠ 老式 `ContextMenu`（基类 `Menu`）**没有 `OwnerDraw` 属性**，自绘是逐 `MenuItem` 开的，
菜单外框那圈仍由系统画 —— 这是当初选它而不是 `ContextMenuStrip` 的代价
（后者的 `Show()` 不阻塞、又不挂 owner，会把鼠标捕获抢走，界面看着像死了）。

## [v1.5.0] - 2026-09-22 19:29

**主题：去掉自绘标题栏（标签条顶到最上面）+ 独立的设置窗口 + 新建标签页秒开**

### 修的bug

1. **Ctrl+1..9 没实现**。README里写着，但代码里根本没有这个键 —— 标签条只有鼠标点击。
   现在补上：`WinEHook` 加 `GotoTabKey`（只在「我们进程是前台」时接管，标准键盘1..9，
   带Shift的不抢），`EmbedForm.GotoTab`切到第N个标签；焦点在我们自己控件上时
   `OnPreviewKeyDown` 也留了一条兜底路径。
2. **标签显示改回一行**（上一轮做成两行是改错了）。标签上只放**文件夹名**，
   完整路径进**悬停提示**；名字和路径一样时（「此电脑」这种）提示里只留一个、不重复两遍。
   标签条高度从44掉回34。
3. **悬停提示的底色/字体没跟颜色模式**。系统那套提示（comctl32）开着视觉样式时经常不认
   `TTM_SETTIPBKCOLOR`，所以改成 `OwnerDraw` 自己画（`Theme.StyleTip`）：
   底色、边框、文字全走我们的调色板，深浅都成立；颜色模式一改，下次弹出来就是新配色。
4. **新建标签页复制了当前标签**。真凶是 `NewTabClicked` / `HotkeyNewTab` / 空白右键三处
   都传了 `CurrentPath()`。改成一律开「此电脑」—— 浏览器的 + 号就是这个意思。
5. **主窗口不区分激活/未激活**（原来永远是设置的那套底色）。现在 `WM_NCACTIVATE` 一到，
   外壳底、标签条底、选中标签底、收藏夹栏底一起换成「失活版」（`Theme.ChromeOff` 等）。
   差值故意做小 —— 我们这几行颜色得和嵌进来的explorer功能区连成一片，差太多反而露接缝。

### 增强

1. **新建标签页不再等一秒多**。日志实测：点 + 到内容出来约1.7秒，其中
   「起explorer进程 + 等它把文件列表建出来」占掉0.8秒左右 —— 这两步跟用户按没按 + 号毫无关系，
   于是提前做掉：每批标签都起完之后，后台（也走串行队列）再起一个**藏着的**备用窗口备着，
   加载完也不显示；一按 + / Ctrl+T 就把它直接显出来，几乎瞬时。用掉立刻再备一个。
   （只在目标是「此电脑」时才用得上 —— 而「新建标签页」现在就是「此电脑」。）
2. **CHANGELOG的时间精确到分钟**（`YYYY-MM-DD HH:MM`），已按各版本的git提交时间回填。

### 新增

1. **开机自启**设置项。写的是当前用户的启动项（`HKCU\...\Run` 里一个 `TabbedExplorer` 值，
   不需要管理员权限），启动命令带 `--tray`：只常驻托盘 + 装 Win+E 钩子，**不弹窗口**。
   ⚠ 状态**只认注册表**，不在 `settings.json` 里再存一份 —— 两份状态迟早对不上
   （用户自己在「任务管理器 → 启动」里禁用的时候），那时菜单里打的勾就是假的。
   另外每次启动会把启动项里的exe路径刷成现在这个，程序目录被挪过也能对上。
2. **标签页右键新增「复制标签页」**（复制一份同路径的标签）。
3. **独立的设置窗口**。齿轮按钮和标签条空白处右键的「设置」现在开**窗口**，
   里面写程序名、版本、简介，设置项也在里面改；**托盘图标右键那份菜单保持原样**。
   内容不另写一份：窗口和托盘菜单都由 `SettingsMenu.Spec` 渲染
   （同 `Group` 的相邻叶子画成一组单选、单独的叶子画成勾选框、带子项的画成下拉框），
   以后加设置项不会漏一边。

### 评估结论：自绘标题栏去掉了

用户问「左上角按钮功能没实现，能不能完整捕获原资源管理器……能实现就可以去掉自己模仿的图标按钮」。

- **原来那条标题栏拿不回来**：嵌进来的explorer是子窗口，子窗口没有非客户区，
  而它的快速访问工具栏恰好画在那一条里 —— 清掉 `WS_CAPTION` 之后那条就空了，
  实测把 `WS_CAPTION` 加回去也毫无变化（顶部那条45px是空的，我们一直在把它裁掉）。
  **所以「完整捕获」做不到。**
- 我们自己仿的那排图标（属性/新建文件夹/撤销/重做/删除/重命名）**确实不灵** ——
  它是「取shell原件的图标 + 把快捷键PostMessage给explorer的焦点窗口」，实测点了没反应。
- 那排图标本来就是多余的：**真正的命令就在嵌进来的explorer自己的功能区里**
  （文件/主页/共享/查看），一模就响。
- **所以：整条自绘标题栏删掉**（`src/TitleBar.cs` 已删除），标签条顶到最上面、多出30像素给内容；
  最小化/最大化还原/关闭三颗并到标签条**最右边**（跟浏览器一样）；
  标签条的**空白处**兼任标题栏 —— 按住拖动可以移动窗口、贴边吸附也白拿
  （超过几个像素才算拖，所以「双击空白 = 新标签」还在）。

### 已知限制

- 嵌进来的explorer是**独立进程**，颜色模式只改得动我们自画的外壳（标签条/收藏夹栏/设置窗口/菜单），
  文件列表仍跟系统 —— 改不了别人进程的开关。
- 「新建标签页秒开」的代价是**多一个藏着的explorer进程**（几十MB）。它是用掉之后才补的，
  程序只驻留托盘、没开过窗口时不会有。

## [v1.4.0] - 2026-09-22 18:45

**主题：定位成「浏览器式的资源管理器」+ 历史/恢复关闭/收藏夹栏 + 捕获所有打开的文件夹**

用户的原话定了调：「其实我们就是仿照浏览器设计，增强Win10资源管理器的功能。」
这一版把功能往浏览器那套补齐，顺手修掉六个bug。

### 修的bug

1. **只抓Win+E，不抓其他方式打开的文件夹**。原来只监听Win+E这个按键，从开始菜单、桌面双击
   打开的文件夹仍是原生窗口。现在加了一个常驻的「某某窗口刚被显示」系统广播监听
   （复用防闪那个 `WinShowWatcher`），认出**新出现的** `CabinetWClass` 顶层窗口就收成标签
   （`ExplorerHost.Adopt`：不起新进程，直接把现成的窗口收进来，所以不闪、也不延迟）。
   配套三件事：
   - 新增 `EmbedApi` 的**认领登记表**：我们自己起的窗口和用户自己开的窗口会同时被两个监听看见，
     谁先认领谁负责，另一个看到已登记就放手（不然两个标签会抢同一个窗口）。
     排序上还有个坑：同一个显示事件会依次叫到所有监听者、**顺序不保证**，所以Hub那边必须
     `Post` 推迟一轮再判，等标签那边**同步**写完认领表。
   - 只抓启动后**新出现**的窗口（启动时先快照一份现有的），不然「把一个窗口从最小化还原」
     也会被当成新开。
   - **安全底线**：接管的窗口多半属于桌面那个shell `explorer.exe` 进程，
     `Adopt` 第一件事就是把当前所有explorer进程pid快照进 `pidsBefore` ——
     少了这一步，关一个标签就会把整个桌面杀掉。
2. **有时候标签页点开是空的**。根因是**同时开好几个标签时会互相抢窗口**：
   每个标签都用「启动前快照」认自己叫起来的那个窗口，两个标签几乎同时起时，
   A可能把B的窗口扫走并嵌掉，B就永远等不到东西、只剩一个黑洞。
   两条修法：① `FindNewCab` 跳过**已被认领**的窗口；
   ② 新增 `CabMatches` —— 嵌之前读那个窗口的地址栏，跟我们要开的路径对一下，对不上就继续等
   （问不出路径/地址栏还没建出来时一律放行，绝不因为比不了就永远不开）。
   另外**干等25秒失败后自动重试一次**，不再把一个黑标签留在那儿。
3. **未激活时鼠标移到标签上不显示名字**（关闭按钮会变色，说明事件是收到了）。
   是 `ToolTip` 在宿主窗口不是前台时**不弹**。加 `ToolTip.ShowAlways = true` 就对了（标签条+标题栏）。
4. **提示气泡的背景/字体颜色没跟着颜色模式**。原来走 `NotifyIcon.ShowBalloonTip`，
   那个气泡是**系统画的**、配色跟系统主题走。新增 `Toast`（自绘气泡，
   `WS_EX_NOACTIVATE` + `ShowWithoutActivation`，不抢焦点，几秒自动消失，点一下也关），
   所有提示改走它。
5. **齿轮点了还是卡死**。上一版判错了：真正的根是 `ContextMenuStrip` **不挂owner**（非模态+抢鼠标捕获），
   日志照样打「弹菜单返回」，从外面看就是界面死了。现在齿轮改用**跟托盘同一个类** `ContextMenu` +
   `Show(owner, point)` —— 走WinForms的模态菜单循环，一直阻塞到菜单关掉，
   跟托盘那条已经在真机上验证没问题的路径完全一致。
6. **未激活时窗口颜色没和Windows原本的逻辑匹配**。收到 `WM_NCACTIVATE` 就切
   自绘标题栏和标签条的 `Inactive`：标题字降灰（`Theme.TextInactive`）、
   选中标签顶上那条强调色变暗（`Theme.AccentDim`），跟原生标题栏失活一个观感。
7. **关标签后在桌面上留一个看不见的孤儿窗口**（实测发现）。`explorer.exe /n,/separate` 起的窗口
   有时会落在**启动前就存在的 explorer 进程**里（很可能就是桌面那个 shell）——
   出于安全我们**绝不能杀那个进程**，但原来那种情况只把窗口「还原成顶层窗口」就完事，
   于是它变成一个不可见的顶层窗口留在桌面上（累积下来还会进下一次启动的捕获基线）。
   改法：关标签时统一判断「这个进程会不会被杀」，不会杀（接管的 / 复用了老进程的）
   就 `WM_CLOSE` 把**窗口**关掉，会杀的就让它跟着进程一起没。

### 增强

- **标签条上的每个图标都有悬停提示**（含快捷键，如「新建标签页(Ctrl+T)」「历史记录(Ctrl+H)」）。
- **标签两行**：第一行文件夹名（加粗），第二行完整路径（长了走 `PathEllipsis`，从中间省）。
- **标签右键菜单**：复制文件夹名 / 复制完整路径 / 在新标签页打开 / 重新打开刚关闭的标签页 / 关闭标签页。
- **配置项改成JSON格式**（`settings.json`）。JSON不支持注释，说明写在 `_` 开头的键里
  （合法JSON，打开就能看懂）。老 `settings.txt` 自动迁移、改名成 `.migrated` 留着不删。

### 新增

- **Ctrl+H 历史记录**：新增 `History`（记去过哪些文件夹，去重、最近的在最前，
  存 `data\history.txt`），菜单可挑一个直接开成标签，也能打开历史文件、清空。
  跟「按桌面记标签」是两码事：那个记的是**当前开着哪些标签**，这个记的是**一路去过哪儿**。
- **Ctrl+Shift+T 恢复关闭的标签页**：关标签时把路径压进栈（后进先出，最多20个），
  点一下恢复最近关的、再点一下恢复上上个。
- **Ctrl+Shift+B 收藏夹栏**：新增 `FavBar`，夹在标签条和内容之间，直接读**系统那个收藏夹**
  （`%USERPROFILE%\Links`）—— 在资源管理器里往「收藏夹」拖个文件夹，这边切一下就能看到，
  不另搞一套收藏数据。快捷方式（`.lnk`）会解析出真实目标
  （不走COM：扫 `LinkInfo` 里的本地路径字符串再验一下存在，扫不到就原样交给explorer自己去解析）。
- 这三个按钮放在**齿轮左边**，齿轮用一条竖线单独隔开（跟Edge一样：扩展在左、头像在右）。
- **标签条空白处右键**也有这三个功能的菜单。
- 设置里新增两项：`favbar`（收藏夹栏）、`captureall`（是否把从开始菜单/桌面打开的文件夹也收成标签）。

### 已知限制

- 强制浅/深色只改得动我们自绘的外壳；嵌进来的explorer是独立进程，文件列表仍跟系统主题。

## [v1.3.0] - 2026-09-22 17:40

**主题：设置面板成型 + 数据全走程序目录（绿色便携）**

修的两个 bug：
- **点齿轮卡死**：原来是**在鼠标按下（MouseDown）里同步弹 `ContextMenuStrip`，
  还挂了 owner 控件**。鼠标消息没走完就切鼠标捕获，菜单的 modal 过滤器跟控件的捕获打架，
  表现就是进程还在、界面全不响应（日志正好停在「点击设置按钮」那一行，没有任何异常）。
  改法两条，跟本项目里早就能跑的那套写法对齐：① 用 `BeginInvoke` 排到当前消息处理完再弹；
  ② **不挂 owner，直接给屏幕坐标 `m.Show(screenPoint)`**，并在弹出前后各打一行日志（下次万一再卡，一眼看出卡在哪一步）。
  > ⚠ **2026-09-22 更正**：这一版的判断是错的，`v1.4.0` 才找到真根因。
  > 「不挂 owner 直接给屏幕坐标」恰恰是**病因**（那个组合是非模态的、还抢鼠标捕获），
  > 而且它让 `Show` 立刻返回、日志一切正常，所以查日志什么都看不出来。
  > 真正的修法是回到 `ContextMenu`（跟托盘同一个类）+ `Show(owner, point)` 的模态循环，
  > 详见 `v1.4.0`。
- **托盘右键没有设置项**：齿轮和托盘的菜单原来各写了一遍，托盘那份漏了。
  现在两边都从 `SettingsMenu` 生成**同一份内容**（齿轮走 `ContextMenuStrip`，托盘走 `MenuItem`），
  以后加项不会再漏一边。托盘菜单结构建一次，设置变了只刷文字（勾选前缀是文字）。

新增/优化：
- **鼠标停在标签上显示完整文件夹名**（标签条里的标题被省略号截了也能看全），提示贴着标签下边弹。
- **设置项扩到五项**（`settings.txt`，`v2` 格式；老 v1 文件照样读）：
  `capture`（捕获方式）/ `keeptabs`（是否保留标签页）/ `theme`（颜色模式）/
  `tabwidth`（标签页宽度 64~240）/ `tabautofit`（是否自适应宽度）。
  菜单里：捕获方式、颜色模式（跟随系统 / 浅色 / 深色）、保留标签页、
  标签页宽度（预设 80~192）、自适应宽度。想要别的宽度直接改 `settings.txt`。
- **绿色便携**：设置、标签记忆、日志全部从 `%APPDATA%\TabbedExplorer` 搬到
  **程序目录下的 `data\`**（新增 `AppPaths`），整个文件夹拷走就能带走；
  程序目录写不动（比如放进 Program Files）时自动退回 `%APPDATA%`。
  第一次用新位置会把老的 `desktops.txt` / `settings.txt` 抄过来（只补缺，不覆盖）。
- 颜色模式的**已知限制**：嵌进来的 explorer 是**独立进程**、按系统主题画自己那一片，
  我们改不了别人进程的开关。所以强制浅/深只改得动我们自己画的外壳（标题栏/标签条/菜单），
  文件列表仍跟系统 —— 设置里点了会明确提示这一点。

## [v1.2.0] - 2026-09-22 17:21

**主题：标签条改浏览器式 + 捕获方式可选**

- **「+」跟着标签走**：新建按钮不再钉在整条最右边，而是紧跟在最后一个标签右侧
  （浏览器就是这样），增删标签、拖拽排序、窗口改宽都会跟着动；一个标签都没有时贴左边。
- **最右边固定一枚设置按钮**（自绘齿轮，带 hover）。菜单第一项就是**捕获方式**：
  - `按虚拟桌面分别捕获` —— v1.1.0 的行为，每张桌面各一个窗口、各记一套标签（默认）；
  - `捕获并迁移到当前桌面` —— v1.0.0 的行为，全进程只一个窗口，Win+E 时把它搬到当前桌面。
  选择存在 `%APPDATA%\TabbedExplorer\settings.txt`（`capture=perdesktop|migrate`），
  切换立刻生效：记忆会「搬家」（只在目标桶还空着时才搬，不覆盖已有的），
  切到迁移模式时只留前台那个窗口、其余收掉（标签已各自落进自己桌面的桶里）。
- **去掉底部那条文件夹名**（状态栏）：标签上已经写着，内容区直接吃到窗口底。
- **外壳底色统一成标签条那种深黑**：自绘标题栏、无边框窗口四周那圈、内容容器底
  全部改用同一个 `Theme.Chrome`（深色下 = 纯黑），消掉标题栏那一行原本的灰接缝。
- 新增 `Settings`（读写 `settings.txt`）、`DesktopHub` 增加 `SetCaptureMode` / `RememberNow`。

## [v1.1.0] - 2026-09-22 16:53

**主题：每张虚拟桌面一个窗口 + 按桌面记忆标签**

把 v1.0.0 那条「窗口跟过去」改成**每张虚拟桌面各自一个窗口、各自一组标签**，
并且退出后按桌面记住标签，下次打开还原。

- **每桌面一个窗口**：托盘图标 + 键盘钩子搬到进程级的 `DesktopHub`
  （全进程只能有一份，必须住在窗口之外）；Win+E 先问「现在在哪张桌面」，
  再给那张桌面建/找回它自己的那个窗口。窗口**不再被搬来搬去** ——
  「按 Win+E 反而被拽到另一张桌面」从结构上不可能再发生。
- **按桌面记忆标签**：`%APPDATA%\TabbedExplorer\desktops.txt`，按虚拟桌面 GUID 分段、
  一行一个标签（`*` 标当时选中的那个）。标签有变动就攒 800ms 落盘，退出前再存一次。
  这一轮没去过的桌面，读进来什么样就写回去什么样，不会被清空。
- **记的是「他现在在哪儿」，不是「我当初让他开哪儿」**：读地址栏那个 `ToolbarWindow32`
  的窗口文本（实测就是 `地址: D:\Dev\...`，真目录就是完整路径）。
  为此试过并且否掉的两条路：ShellWindows / `IWebBrowser2.LocationURL`
  （窗口被 SetParent 之后它回报的 HWND 变成我们的顶层窗口，一张桌面上的标签全撞成一个 key）、
  `AccessibleObjectFromWindow(OBJID_NATIVEOM)`（CabinetWClass 上直接 E_FAIL）。
- **标签图标改成当前文件夹的实时图标**：直接读窗口自身挂的 `WM_GETICON`，导航后会自己变。
- **托盘/任务栏用程序自己的图标**：`app.ico` 编进 exe，不再借 shell32 的「新建文件夹」。
- 还原时**开不了的记忆项会被跳过**（库 / 虚拟文件夹的地址栏给的是显示名，没有真实路径），
  其余标签照常还原。

**已知未做**：库（比如「视频」）的地址栏给的是显示名而不是路径，那种标签还原时会被跳过。

## [v1.0.0] - 2026-09-22 16:31

**主题：切换桌面捕获**

第一个可用版本。Win10 上带标签的资源管理器：每个标签是一个**真的 explorer 窗口**
（`explorer.exe /n,/separate` 起独立进程，再降级成子窗口 `SetParent` 嵌进容器），
所以文件列表 / 右键菜单 / 拖放 / 缩略图 / 功能区**全部是系统原件**，外观与原生一致。

- **接 Win+E**：常驻后台（托盘 + `WH_KEYBOARD_LL` 钩子），关窗口不等于退程序。
- **按虚拟桌面捕获**：在哪个桌面按 Win+E，就在那个桌面开/合并标签，退出后按桌面记住标签页。
- **开标签不闪**：发现新 explorer 窗口立刻 `SW_HIDE`（`EVENT_OBJECT_SHOW` 系统广播，0ms 级），
  等文件列表建好再作为子窗口 `SW_SHOW`。
- **不把人拽走**：抢前台之前先确认窗口确实在当前桌面（未公开 `GetCurrentDesktop` + 回读复核），
  确认不了就只显示不激活。
- 标签：左边是**当前文件夹的实时图标**（直接读窗口自身的 `WM_GETICON`，与 shell 给该文件夹的图标逐像素一致）、
  宽度 112/72 逻辑像素、底色纯黑与 explorer 功能区同色、可拖拽排序、中键关闭。
- 快捷键：Ctrl+T 新标签、Ctrl+W 关标签、Ctrl+Tab 切换、Ctrl+1..9 跳转、
  Ctrl+L 地址栏、Alt+←/→ 前后退、Alt+↑ 上一级、F5 刷新。
- 「此电脑」按路径去重：连按 Win+E 不会刷出一排「此电脑」。

**已知未做**：每个虚拟桌面目前是「窗口跟过去」而不是「各桌面各自常驻一个窗口/一组标签」。
