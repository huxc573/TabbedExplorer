# 变更记录

版本号唯一来源 = 仓库根 `VERSION` 文件（单行），标签 `v<X.Y.Z>`（附注标签）。
口径：新功能抬 MINOR，修 bug 只走 PATCH，不兼容才抬 MAJOR。

## [v1.6.0] - 2026-09-22 20:22

**主题：数据全改 JSON + 右键菜单复活 + 收藏夹自己管 + 自适应宽度拆两档 + 深浅色来回切不再花屏**

### 修的bug

1. **只有 `settings` 用了 JSON，桌面记忆/历史记录还是 txt**。现在四个数据文件统一走 JSON：
   `settings.json` / `desktops.json` / `history.json` / `favorites.json`；
   零依赖的手写解析器 `Json.cs` 由四边共用。老 `desktops.txt` / `history.txt` 第一次跑时读进来、
   原文件改名成 `.txt.migrated` 留底。`log.txt` 保持 txt —— 它就是给人看的。
2. **空白处右键、标签右键都没反应**。真凶：菜单弹在 `MouseDown` 里，
   紧接着到达的右键**抬起**消息被刚弹出的模态菜单（它持有鼠标捕获）收走，菜单于是立刻自己关了。
   全部改到 `MouseUp` 里弹；并且所有弹出统一走 `MenuFx.Show` ——
   **弹之前 / 关之后各打一行日志**，下次再有「点了没反应」直接看日志，不用猜。
3. **历史记录点了不开目录**。历史项的回调原来挂着空实现。现在接 `OpenFromHistory`：
   去重、不可还原的项（回收站/搜索结果页）不给点、点了有日志。
4. **从桌面 / 开始菜单打开文件夹还是闪一下**（Win+E 那条路不闪）。
   原因是「发现窗口」和「藏起来」之间隔了一次轮询。`TryCapture` 改成**一发现立刻 `SW_HIDE`**，
   并记下「这个是我藏的」；万一最后没被认领（不是我们的窗口），
   `ReleaseIfAbandoned` 会把它显示回去 —— 不能因为藏错了就在桌面上留个看不见的窗口。
5. **标签标题不该加粗**。标签用常规体；悬停提示的**第一行**（文件夹名）才加粗，路径不加粗。
6. **颜色模式切来切去会「一半深一半浅」**。两个原因一起修：
   ① `OnThemeChanged` 碰到「跟随系统」直接 `return` ——「强制深色 → 跟随系统（而系统是浅色）」
   这一步只把外壳刷成浅色、嵌进来的 explorer 还停在深色。
   现在**每次都重上一遍**（「跟随系统」不是「不用管」，它只是把目标值换成当前系统值）。
   ② `SetWindowTheme` 只在子应用名**真的变了**时才让窗口重画，来回切会撞上「目标值=现状」那一步
   （它觉得无事可做，旧像素就留在屏幕上）。现在先设成**反的那一边**再设目标，
   保证这一轮真发生两次切换；最后再对整棵子树 `RedrawWindow`（异步，不等它画完）。
   预热好的备用窗口也一起重上（它不在 `hosts` 里，漏了就是下个新标签页颜色不对）。
7. **设置里的「记住当前标签」说不清自己是干嘛的**（川直接问了）。标签改成
   「立即记住当前标签（平时自动记，这个是手动存一次）」。

### 增强

1. **自适应宽度拆成两档**（原来一个开关管两件事，说不清）：
   「名称过长时自动加宽」（按每个标签各自的标题宽度算，夹在最小/最大之间）、
   「挤不下时自动缩窄」（不够就均分）。
   **两个都关**的时候不再横向溢出：标签总宽被夹在「右边那几个按钮之前」，
   放不下的靠横向滚动 —— 不会盖住设置按钮。
2. **滚轮**（原来在哪儿都不灵，因为内容区是**跨进程**嵌进来的窗口，
   滚轮消息根本进不了我们的消息循环）：加了全局低级鼠标钩子 `MouseWheelHook`，
   按 `WindowFromPoint` 判断那个窗口归谁 ——
   · 在标签条上 = 切上一个/下一个标签；
   · 在内容区、且标签条有溢出 = 横向滚动标签条。
   登记/查表走 `WheelRouter`；钩子线程只做「读状态 + 投回 UI 线程」，绝不碰界面。
3. **收藏夹栏自己管数据**（原来直接读系统 `%USERPROFILE%\Links`）：
   数据落在 `data\favorites.json`，第一次跑从系统收藏夹**播种**一次，之后自己记。
   最左边加了一枚收藏夹图标（点开数据目录）；**可以把文件夹 / 文件直接拖进来**；
   栏上右键可「在新标签页打开 / 用默认程序打开 / 复制完整路径 / 从收藏夹移除 / 刷新 / 打开数据目录 / 隐藏」。
4. **非重要变更不再弹右下角提示**。改设置、存记忆这类成功路径全部静音，只有**出错**才弹 ——
   这样弹出来就一定是真出事了。
5. **非激活标签省内存**：切走 3 秒还没被切回来的标签，对它那个独立 explorer 进程
   `EmptyWorkingSet`（只把驻留页换出，进程 / 窗口 / 里面的状态一点都不动）。
   收编来的窗口不碰 —— 那种往往跟桌面外壳共用进程，动了整台机器都有感觉。
   延时 3 秒是为了别在 Ctrl+Tab 来回切时反复「收了又读回来」。

### 菜单对齐

「显示收藏夹栏」这项原来靠文字前缀 `✓ ` 表示勾上 —— 比同级项多两个字符，看着就是没对齐
（川原话「显示收藏夹里没和其它选项一样居左对齐」）。改成 `MenuItem.Checked` +
自绘的**固定宽度勾选列**（`MenuFx`），勾和不勾的文字起点完全一致，
顺便把「深色下系统那个小方块勾了看不见」一起解决。
⚠ 老式 `ContextMenu`（基类 `Menu`）**没有 `OwnerDraw` 属性**，自绘是逐 `MenuItem` 开的，
菜单外框那圈仍由系统画 —— 这是当初选它而不是 `ContextMenuStrip` 的代价
（后者的 `Show()` 不阻塞、又不挂 owner，会把鼠标捕获抢走，界面看着像死了）。

## [v1.5.0] - 2026-09-22 19:29

**主题：去掉自绘标题栏（标签条顶到最上面）+ 独立的设置窗口 + 新建标签页秒开**

### 修的bug

1. **Ctrl+1..9 没实现**。README里写着，但代码里根本没有这个键 —— 标签条只有鼠标点击。
   现在补上：`WinEHook` 加 `GotoTabKey`（只在「我们进程是前台」时接管，标准键盘1..9，
   带Shift的不抢），`EmbedForm.GotoTab`切到第N个标签；焦点在我们自己控件上时
   `OnPreviewKeyDown` 也留了一条兜底路径。
2. **标签显示改回一行**（上一轮做成两行是改错了）。标签上只放**文件夹名**，
   完整路径进**悬停提示**；名字和路径一样时（「此电脑」这种）提示里只留一个、不重复两遍。
   标签条高度从44掉回34。
3. **悬停提示的底色/字体没跟颜色模式**。系统那套提示（comctl32）开着视觉样式时经常不认
   `TTM_SETTIPBKCOLOR`，所以改成 `OwnerDraw` 自己画（`Theme.StyleTip`）：
   底色、边框、文字全走我们的调色板，深浅都成立；颜色模式一改，下次弹出来就是新配色。
4. **新建标签页复制了当前标签**。真凶是 `NewTabClicked` / `HotkeyNewTab` / 空白右键三处
   都传了 `CurrentPath()`。改成一律开「此电脑」—— 浏览器的 + 号就是这个意思。
5. **主窗口不区分激活/未激活**（原来永远是设置的那套底色）。现在 `WM_NCACTIVATE` 一到，
   外壳底、标签条底、选中标签底、收藏夹栏底一起换成「失活版」（`Theme.ChromeOff` 等）。
   差值故意做小 —— 我们这几行颜色得和嵌进来的explorer功能区连成一片，差太多反而露接缝。

### 增强

1. **新建标签页不再等一秒多**。日志实测：点 + 到内容出来约1.7秒，其中
   「起explorer进程 + 等它把文件列表建出来」占掉0.8秒左右 —— 这两步跟用户按没按 + 号毫无关系，
   于是提前做掉：每批标签都起完之后，后台（也走串行队列）再起一个**藏着的**备用窗口备着，
   加载完也不显示；一按 + / Ctrl+T 就把它直接显出来，几乎瞬时。用掉立刻再备一个。
   （只在目标是「此电脑」时才用得上 —— 而「新建标签页」现在就是「此电脑」。）
2. **CHANGELOG的时间精确到分钟**（`YYYY-MM-DD HH:MM`），已按各版本的git提交时间回填。

### 新增

1. **开机自启**设置项。写的是当前用户的启动项（`HKCU\...\Run` 里一个 `TabbedExplorer` 值，
   不需要管理员权限），启动命令带 `--tray`：只常驻托盘 + 装 Win+E 钩子，**不弹窗口**。
   ⚠ 状态**只认注册表**，不在 `settings.json` 里再存一份 —— 两份状态迟早对不上
   （川自己在「任务管理器 → 启动」里禁用的时候），那时菜单里打的勾就是假的。
   另外每次启动会把启动项里的exe路径刷成现在这个，程序目录被挪过也能对上。
2. **标签页右键新增「复制标签页」**（复制一份同路径的标签）。
3. **独立的设置窗口**。齿轮按钮和标签条空白处右键的「设置」现在开**窗口**，
   里面写程序名、版本、简介，设置项也在里面改；**托盘图标右键那份菜单保持原样**。
   内容不另写一份：窗口和托盘菜单都由 `SettingsMenu.Spec` 渲染
   （同 `Group` 的相邻叶子画成一组单选、单独的叶子画成勾选框、带子项的画成下拉框），
   以后加设置项不会漏一边。

### 评估结论：自绘标题栏去掉了

川问「左上角按钮功能没实现，能不能完整捕获原资源管理器……能实现就可以去掉自己模仿的图标按钮」。

- **原来那条标题栏拿不回来**：嵌进来的explorer是子窗口，子窗口没有非客户区，
  而它的快速访问工具栏恰好画在那一条里 —— 清掉 `WS_CAPTION` 之后那条就空了，
  实测把 `WS_CAPTION` 加回去也毫无变化（顶部那条45px是空的，我们一直在把它裁掉）。
  **所以「完整捕获」做不到。**
- 我们自己仿的那排图标（属性/新建文件夹/撤销/重做/删除/重命名）**确实不灵** ——
  它是「取shell原件的图标 + 把快捷键PostMessage给explorer的焦点窗口」，实测点了没反应。
- 那排图标本来就是多余的：**真正的命令就在嵌进来的explorer自己的功能区里**
  （文件/主页/共享/查看），一模就响。
- **所以：整条自绘标题栏删掉**（`src/TitleBar.cs` 已删除），标签条顶到最上面、多出30像素给内容；
  最小化/最大化还原/关闭三颗并到标签条**最右边**（跟浏览器一样）；
  标签条的**空白处**兼任标题栏 —— 按住拖动可以移动窗口、贴边吸附也白拿
  （超过几个像素才算拖，所以「双击空白 = 新标签」还在）。

### 已知限制

- 嵌进来的explorer是**独立进程**，颜色模式只改得动我们自画的外壳（标签条/收藏夹栏/设置窗口/菜单），
  文件列表仍跟系统 —— 改不了别人进程的开关。
- 「新建标签页秒开」的代价是**多一个藏着的explorer进程**（几十MB）。它是用掉之后才补的，
  程序只驻留托盘、没开过窗口时不会有。

## [v1.4.0] - 2026-09-22 18:45

**主题：定位成「浏览器式的资源管理器」+ 历史/恢复关闭/收藏夹栏 + 捕获所有打开的文件夹**

川的原话定了调：「其实我们就是仿照浏览器设计，增强Win10资源管理器的功能。」
这一版把功能往浏览器那套补齐，顺手修掉六个bug。

### 修的bug

1. **只抓Win+E，不抓其他方式打开的文件夹**。原来只监听Win+E这个按键，从开始菜单、桌面双击
   打开的文件夹仍是原生窗口。现在加了一个常驻的「某某窗口刚被显示」系统广播监听
   （复用防闪那个 `WinShowWatcher`），认出**新出现的** `CabinetWClass` 顶层窗口就收成标签
   （`ExplorerHost.Adopt`：不起新进程，直接把现成的窗口收进来，所以不闪、也不延迟）。
   配套三件事：
   - 新增 `EmbedApi` 的**认领登记表**：我们自己起的窗口和川自己开的窗口会同时被两个监听看见，
     谁先认领谁负责，另一个看到已登记就放手（不然两个标签会抢同一个窗口）。
     排序上还有个坑：同一个显示事件会依次叫到所有监听者、**顺序不保证**，所以Hub那边必须
     `Post` 推迟一轮再判，等标签那边**同步**写完认领表。
   - 只抓启动后**新出现**的窗口（启动时先快照一份现有的），不然「把一个窗口从最小化还原」
     也会被当成新开。
   - **安全底线**：接管的窗口多半属于桌面那个shell `explorer.exe` 进程，
     `Adopt` 第一件事就是把当前所有explorer进程pid快照进 `pidsBefore` ——
     少了这一步，关一个标签就会把整个桌面杀掉。
2. **有时候标签页点开是空的**。根因是**同时开好几个标签时会互相抢窗口**：
   每个标签都用「启动前快照」认自己叫起来的那个窗口，两个标签几乎同时起时，
   A可能把B的窗口扫走并嵌掉，B就永远等不到东西、只剩一个黑洞。
   两条修法：① `FindNewCab` 跳过**已被认领**的窗口；
   ② 新增 `CabMatches` —— 嵌之前读那个窗口的地址栏，跟我们要开的路径对一下，对不上就继续等
   （问不出路径/地址栏还没建出来时一律放行，绝不因为比不了就永远不开）。
   另外**干等25秒失败后自动重试一次**，不再把一个黑标签留在那儿。
3. **未激活时鼠标移到标签上不显示名字**（关闭按钮会变色，说明事件是收到了）。
   是 `ToolTip` 在宿主窗口不是前台时**不弹**。加 `ToolTip.ShowAlways = true` 就对了（标签条+标题栏）。
4. **提示气泡的背景/字体颜色没跟着颜色模式**。原来走 `NotifyIcon.ShowBalloonTip`，
   那个气泡是**系统画的**、配色跟系统主题走。新增 `Toast`（自绘气泡，
   `WS_EX_NOACTIVATE` + `ShowWithoutActivation`，不抢焦点，几秒自动消失，点一下也关），
   所有提示改走它。
5. **齿轮点了还是卡死**。上一版判错了：真正的根是 `ContextMenuStrip` **不挂owner**（非模态+抢鼠标捕获），
   日志照样打「弹菜单返回」，从外面看就是界面死了。现在齿轮改用**跟托盘同一个类** `ContextMenu` +
   `Show(owner, point)` —— 走WinForms的模态菜单循环，一直阻塞到菜单关掉，
   跟托盘那条已经在真机上验证没问题的路径完全一致。
6. **未激活时窗口颜色没和Windows原本的逻辑匹配**。收到 `WM_NCACTIVATE` 就切
   自绘标题栏和标签条的 `Inactive`：标题字降灰（`Theme.TextInactive`）、
   选中标签顶上那条强调色变暗（`Theme.AccentDim`），跟原生标题栏失活一个观感。
7. **关标签后在桌面上留一个看不见的孤儿窗口**（实测发现）。`explorer.exe /n,/separate` 起的窗口
   有时会落在**启动前就存在的 explorer 进程**里（很可能就是桌面那个 shell）——
   出于安全我们**绝不能杀那个进程**，但原来那种情况只把窗口「还原成顶层窗口」就完事，
   于是它变成一个不可见的顶层窗口留在桌面上（累积下来还会进下一次启动的捕获基线）。
   改法：关标签时统一判断「这个进程会不会被杀」，不会杀（接管的 / 复用了老进程的）
   就 `WM_CLOSE` 把**窗口**关掉，会杀的就让它跟着进程一起没。

### 增强

- **标签条上的每个图标都有悬停提示**（含快捷键，如「新建标签页(Ctrl+T)」「历史记录(Ctrl+H)」）。
- **标签两行**：第一行文件夹名（加粗），第二行完整路径（长了走 `PathEllipsis`，从中间省）。
- **标签右键菜单**：复制文件夹名 / 复制完整路径 / 在新标签页打开 / 重新打开刚关闭的标签页 / 关闭标签页。
- **配置项改成JSON格式**（`settings.json`）。JSON不支持注释，说明写在 `_` 开头的键里
  （合法JSON，打开就能看懂）。老 `settings.txt` 自动迁移、改名成 `.migrated` 留着不删。

### 新增

- **Ctrl+H 历史记录**：新增 `History`（记去过哪些文件夹，去重、最近的在最前，
  存 `data\history.txt`），菜单可挑一个直接开成标签，也能打开历史文件、清空。
  跟「按桌面记标签」是两码事：那个记的是**当前开着哪些标签**，这个记的是**一路去过哪儿**。
- **Ctrl+Shift+T 恢复关闭的标签页**：关标签时把路径压进栈（后进先出，最多20个），
  点一下恢复最近关的、再点一下恢复上上个。
- **Ctrl+Shift+B 收藏夹栏**：新增 `FavBar`，夹在标签条和内容之间，直接读**系统那个收藏夹**
  （`%USERPROFILE%\Links`）—— 在资源管理器里往「收藏夹」拖个文件夹，这边切一下就能看到，
  不另搞一套收藏数据。快捷方式（`.lnk`）会解析出真实目标
  （不走COM：扫 `LinkInfo` 里的本地路径字符串再验一下存在，扫不到就原样交给explorer自己去解析）。
- 这三个按钮放在**齿轮左边**，齿轮用一条竖线单独隔开（跟Edge一样：扩展在左、头像在右）。
- **标签条空白处右键**也有这三个功能的菜单。
- 设置里新增两项：`favbar`（收藏夹栏）、`captureall`（是否把从开始菜单/桌面打开的文件夹也收成标签）。

### 已知限制

- 强制浅/深色只改得动我们自绘的外壳；嵌进来的explorer是独立进程，文件列表仍跟系统主题。

## [v1.3.0] - 2026-09-22 17:40

**主题：设置面板成型 + 数据全走程序目录（绿色便携）**

修的两个 bug：
- **点齿轮卡死**：原来是**在鼠标按下（MouseDown）里同步弹 `ContextMenuStrip`，
  还挂了 owner 控件**。鼠标消息没走完就切鼠标捕获，菜单的 modal 过滤器跟控件的捕获打架，
  表现就是进程还在、界面全不响应（日志正好停在「点击设置按钮」那一行，没有任何异常）。
  改法两条，跟本项目里早就能跑的那套写法对齐：① 用 `BeginInvoke` 排到当前消息处理完再弹；
  ② **不挂 owner，直接给屏幕坐标 `m.Show(screenPoint)`**，并在弹出前后各打一行日志（下次万一再卡，一眼看出卡在哪一步）。
  > ⚠ **2026-09-22 更正**：这一版的判断是错的，`v1.4.0` 才找到真根因。
  > 「不挂 owner 直接给屏幕坐标」恰恰是**病因**（那个组合是非模态的、还抢鼠标捕获），
  > 而且它让 `Show` 立刻返回、日志一切正常，所以查日志什么都看不出来。
  > 真正的修法是回到 `ContextMenu`（跟托盘同一个类）+ `Show(owner, point)` 的模态循环，
  > 详见 `v1.4.0`。
- **托盘右键没有设置项**：齿轮和托盘的菜单原来各写了一遍，托盘那份漏了。
  现在两边都从 `SettingsMenu` 生成**同一份内容**（齿轮走 `ContextMenuStrip`，托盘走 `MenuItem`），
  以后加项不会再漏一边。托盘菜单结构建一次，设置变了只刷文字（勾选前缀是文字）。

新增/优化：
- **鼠标停在标签上显示完整文件夹名**（标签条里的标题被省略号截了也能看全），提示贴着标签下边弹。
- **设置项扩到五项**（`settings.txt`，`v2` 格式；老 v1 文件照样读）：
  `capture`（捕获方式）/ `keeptabs`（是否保留标签页）/ `theme`（颜色模式）/
  `tabwidth`（标签页宽度 64~240）/ `tabautofit`（是否自适应宽度）。
  菜单里：捕获方式、颜色模式（跟随系统 / 浅色 / 深色）、保留标签页、
  标签页宽度（预设 80~192）、自适应宽度。想要别的宽度直接改 `settings.txt`。
- **绿色便携**：设置、标签记忆、日志全部从 `%APPDATA%\TabbedExplorer` 搬到
  **程序目录下的 `data\`**（新增 `AppPaths`），整个文件夹拷走就能带走；
  程序目录写不动（比如放进 Program Files）时自动退回 `%APPDATA%`。
  第一次用新位置会把老的 `desktops.txt` / `settings.txt` 抄过来（只补缺，不覆盖）。
- 颜色模式的**已知限制**：嵌进来的 explorer 是**独立进程**、按系统主题画自己那一片，
  我们改不了别人进程的开关。所以强制浅/深只改得动我们自己画的外壳（标题栏/标签条/菜单），
  文件列表仍跟系统 —— 设置里点了会明确提示这一点。

## [v1.2.0] - 2026-09-22 17:21

**主题：标签条改浏览器式 + 捕获方式可选**

- **「+」跟着标签走**：新建按钮不再钉在整条最右边，而是紧跟在最后一个标签右侧
  （浏览器就是这样），增删标签、拖拽排序、窗口改宽都会跟着动；一个标签都没有时贴左边。
- **最右边固定一枚设置按钮**（自绘齿轮，带 hover）。菜单第一项就是**捕获方式**：
  - `按虚拟桌面分别捕获` —— v1.1.0 的行为，每张桌面各一个窗口、各记一套标签（默认）；
  - `捕获并迁移到当前桌面` —— v1.0.0 的行为，全进程只一个窗口，Win+E 时把它搬到当前桌面。
  选择存在 `%APPDATA%\TabbedExplorer\settings.txt`（`capture=perdesktop|migrate`），
  切换立刻生效：记忆会「搬家」（只在目标桶还空着时才搬，不覆盖已有的），
  切到迁移模式时只留前台那个窗口、其余收掉（标签已各自落进自己桌面的桶里）。
- **去掉底部那条文件夹名**（状态栏）：标签上已经写着，内容区直接吃到窗口底。
- **外壳底色统一成标签条那种深黑**：自绘标题栏、无边框窗口四周那圈、内容容器底
  全部改用同一个 `Theme.Chrome`（深色下 = 纯黑），消掉标题栏那一行原本的灰接缝。
- 新增 `Settings`（读写 `settings.txt`）、`DesktopHub` 增加 `SetCaptureMode` / `RememberNow`。

## [v1.1.0] - 2026-09-22 16:53

**主题：每张虚拟桌面一个窗口 + 按桌面记忆标签**

把 v1.0.0 那条「窗口跟过去」改成**每张虚拟桌面各自一个窗口、各自一组标签**，
并且退出后按桌面记住标签，下次打开还原。

- **每桌面一个窗口**：托盘图标 + 键盘钩子搬到进程级的 `DesktopHub`
  （全进程只能有一份，必须住在窗口之外）；Win+E 先问「现在在哪张桌面」，
  再给那张桌面建/找回它自己的那个窗口。窗口**不再被搬来搬去** ——
  「按 Win+E 反而被拽到另一张桌面」从结构上不可能再发生。
- **按桌面记忆标签**：`%APPDATA%\TabbedExplorer\desktops.txt`，按虚拟桌面 GUID 分段、
  一行一个标签（`*` 标当时选中的那个）。标签有变动就攒 800ms 落盘，退出前再存一次。
  这一轮没去过的桌面，读进来什么样就写回去什么样，不会被清空。
- **记的是「他现在在哪儿」，不是「我当初让他开哪儿」**：读地址栏那个 `ToolbarWindow32`
  的窗口文本（实测就是 `地址: D:\Dev\...`，真目录就是完整路径）。
  为此试过并且否掉的两条路：ShellWindows / `IWebBrowser2.LocationURL`
  （窗口被 SetParent 之后它回报的 HWND 变成我们的顶层窗口，一张桌面上的标签全撞成一个 key）、
  `AccessibleObjectFromWindow(OBJID_NATIVEOM)`（CabinetWClass 上直接 E_FAIL）。
- **标签图标改成当前文件夹的实时图标**：直接读窗口自身挂的 `WM_GETICON`，导航后会自己变。
- **托盘/任务栏用程序自己的图标**：`app.ico` 编进 exe，不再借 shell32 的「新建文件夹」。
- 还原时**开不了的记忆项会被跳过**（库 / 虚拟文件夹的地址栏给的是显示名，没有真实路径），
  其余标签照常还原。

**已知未做**：库（比如「视频」）的地址栏给的是显示名而不是路径，那种标签还原时会被跳过。

## [v1.0.0] - 2026-09-22 16:31

**主题：切换桌面捕获**

第一个可用版本。Win10 上带标签的资源管理器：每个标签是一个**真的 explorer 窗口**
（`explorer.exe /n,/separate` 起独立进程，再降级成子窗口 `SetParent` 嵌进容器），
所以文件列表 / 右键菜单 / 拖放 / 缩略图 / 功能区**全部是系统原件**，外观与原生一致。

- **接 Win+E**：常驻后台（托盘 + `WH_KEYBOARD_LL` 钩子），关窗口不等于退程序。
- **按虚拟桌面捕获**：在哪个桌面按 Win+E，就在那个桌面开/合并标签，退出后按桌面记住标签页。
- **开标签不闪**：发现新 explorer 窗口立刻 `SW_HIDE`（`EVENT_OBJECT_SHOW` 系统广播，0ms 级），
  等文件列表建好再作为子窗口 `SW_SHOW`。
- **不把人拽走**：抢前台之前先确认窗口确实在当前桌面（未公开 `GetCurrentDesktop` + 回读复核），
  确认不了就只显示不激活。
- 标签：左边是**当前文件夹的实时图标**（直接读窗口自身的 `WM_GETICON`，与 shell 给该文件夹的图标逐像素一致）、
  宽度 112/72 逻辑像素、底色纯黑与 explorer 功能区同色、可拖拽排序、中键关闭。
- 快捷键：Ctrl+T 新标签、Ctrl+W 关标签、Ctrl+Tab 切换、Ctrl+1..9 跳转、
  Ctrl+L 地址栏、Alt+←/→ 前后退、Alt+↑ 上一级、F5 刷新。
- 「此电脑」按路径去重：连按 Win+E 不会刷出一排「此电脑」。

**已知未做**：每个虚拟桌面目前是「窗口跟过去」而不是「各桌面各自常驻一个窗口/一组标签」。
